const logger = (title, value) => {
    console.log(`==============${title}===============`)
    console.log(value);
    console.log(`==============${title}===============`)
}

module.exports = {
    logger
}